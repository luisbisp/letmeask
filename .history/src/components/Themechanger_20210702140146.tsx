import Switch from 'react-switch';

export function ThemeChanger(){
return (
    <Switch
    onChange={() => { }}
    checked={true}
    checkedIcon={false}
    uncheckedIcon={false}
    height={10}
    width={40}
    handleDiameter={20}

/>
)
}